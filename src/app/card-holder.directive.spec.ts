import { CardHolderDirective } from './card-holder.directive';

describe('CardHolderDirective', () => {
  it('should create an instance', () => {
    const directive = new CardHolderDirective();
    expect(directive).toBeTruthy();
  });
});
